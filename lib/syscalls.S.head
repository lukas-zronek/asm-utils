#include <syscall.h>

#define ARG_1 8
#define ARG_2 12
#define ARG_3 16
#define ARG_4 20

.text
	.Lkernel:
		int $SYSCALL_VECTOR
        	ret
	.Lerror:
		movl %eax, _errno
		movl $-1, %eax
	.Lreturn:
		movl %ebp, %esp
		popl %ebp
		ret
